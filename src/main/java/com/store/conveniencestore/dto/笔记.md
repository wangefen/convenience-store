record括号 = 这个数据对象需要保存哪些数据

DTO 不只用于“限制修改”，还用于：
接收前端请求
规定接口返回格式
参数校验
避免数据库结构直接暴露给前端
Entity 通常和数据库表对应，但不一定永远严格一对一。例如后续可能添加不入库的临时字段，或者通过关联查询组合数据。不过你目前这个项目，可以先按“Entity 对应数据库表”理解。

一句话总结：

DTO 面向接口，决定前端能传什么、能看到什么；Entity 面向数据库，表示数据库中的完整数据。